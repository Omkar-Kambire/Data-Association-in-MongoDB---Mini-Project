<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-slate-900 text-white p-5">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 p-2 rounded-md mb-5 inline-block" href="/logout">Logout</a>
        </div>
      <h3 class="text-3xl">
        <span class="font-light">Hello</span> <%= user.name %> 👋🏻
      </h3>
      <h5 class="mb-5">You can create a new post</h5>
      <form action="/post" method="post">
        <textarea
          placeholder="What's on your mind?"
          class="block p-3 outline-none 1/3 resize-none bg-transparent border-2 border-slate-600 rounded-md"
          name="content"
        ></textarea>
        <input
          class="mt-2 block bg-blue-500 p-2 rounded-md"
          type="submit"
          value="Create New Post"
        />
      </form>

      <div class="posts mt-20">
            <h3 class="text-slate-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(post => { %>
                    <div class="post w-1/3 mb-4 border-[1px] border-slate-600  bg-slate-800 p-4 rounded-md mb-4">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="tracking-tight"><%= post.content %></p>
                        <div class="btns flex gap-4 mt-5">
                            <a class="text-blue-500" href="">Like</a>
                            <a class="text-slate-500" href="">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
  </body>
</html>
