<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-slate-900 text-white p-5">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 p-2 rounded-md mb-5 inline-block" href="/logout">Logout</a>
        </div>

      <h5 class="mb-5">Edit post</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          placeholder="What's on your mind?"
          class="block p-3 outline-none 1/3 resize-none bg-transparent border-2 border-slate-600 rounded-md"
          name="content"
        ><%= post.content %></textarea>
        <input
          class="mt-2 block bg-blue-500 p-2 rounded-md"
          type="submit"
          value="Update Post"
        />
      </form>
  </body>
</html>
